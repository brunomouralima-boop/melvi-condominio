# Base de dados (SQLite — ficheiro local, sem infra)
DATABASE_URL="file:./dev.db"

# Servidor
PORT=4100
NODE_ENV=development

# Autenticação
JWT_SECRET="troca-este-segredo-em-producao"
JWT_EXPIRES_IN="7d"

# CORS — origem do frontend
CORS_ORIGIN="http://localhost:5173"

# Credenciais iniciais (usadas pelo seed)
SEED_PASSWORD="familia123"
